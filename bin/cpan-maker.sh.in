#!/usr/bin/env bash
#-*- mode: sh; -*-
# modulino invocation

set -euo pipefail

MODULE_NAME=@MODULE_NAME@

module=$(MODULE_NAME="$MODULE_NAME" perl -MModule::Load -e 'load $ENV{MODULE_NAME}; $path = $ENV{MODULE_NAME}; $path =~s/::/\//g; print $INC{"$path.pm"};')

perl $module "$@"

